Build a production-ready data pipeline that scrapes, cleans, and stores all NSE & BSE company data from Screener.in into a PostgreSQL database for internal use.

🧠 SYSTEM OVERVIEW

You are building a scalable, fault-tolerant data ingestion system with:

Scraper (Screener.in)
Data Cleaner & Normalizer
PostgreSQL Storage Layer
Scheduler (for periodic updates)
Logging + Monitoring
Retry + Rate-limit handling

This system must support 4000+ companies and scale to 100–200 concurrent users (read load).

📦 INPUT

We already have:

A list of all NSE & BSE companies (with Screener URLs or symbols)
🎯 REQUIREMENTS
1. DATA TO SCRAPE (from Screener.in)

For each company, extract:

🧾 Company Info
Name
Symbol
Sector / Industry
Market Cap
Current Price
📊 Fundamentals
PE, PB, ROCE, ROE
Debt to Equity
EPS
Dividend Yield
Sales Growth, Profit Growth
📈 Financial Statements
Profit & Loss (yearly + quarterly)
Balance Sheet
Cash Flow
📉 Ratios Section
All available ratios listed
🧠 Insights / Notes
Pros / Cons
About section
📰 News (if available)
Headlines
Dates
Source (if present)
📅 Historical Data
If available via Screener (tables)
🏗️ ARCHITECTURE
🔹 Backend Stack
Python 3.11+
Async scraping (httpx / aiohttp)
HTML parsing: BeautifulSoup or lxml
PostgreSQL (via asyncpg or SQLAlchemy)
Redis (for queue + caching, optional but recommended)
🔹 System Design
1. Scraper Layer
Async workers (concurrent scraping with semaphore)
Rate limit: max 3–5 requests/sec
Retry logic (exponential backoff)
User-Agent rotation
2. Parser Layer
Convert raw HTML → structured JSON
Handle missing fields gracefully
Normalize numeric formats (₹, %, commas)
3. Data Cleaning Layer
Convert strings → numeric types
Standardize keys
Remove duplicates
Add metadata:
scraped_at
data_quality
4. Storage Layer (PostgreSQL)

Design schema:

Table: companies

id
name
symbol
sector
industry
screener_url

Table: fundamentals

company_id (FK)
market_cap
pe
pb
roce
roe
debt_to_equity
dividend_yield
sales_growth
profit_growth
updated_at

Table: financials
(JSONB for flexibility)

company_id
profit_loss (jsonb)
balance_sheet (jsonb)
cash_flow (jsonb)
quarterly (jsonb)

Table: ratios

company_id
ratios_data (jsonb)

Table: insights

company_id
pros (text[])
cons (text[])
about (text)

Table: news

id
company_id
title
date
source

Table: scrape_logs

company_id
status (success/failure)
error_message
scraped_at
⚙️ PIPELINE FLOW
Load company list
Push into queue
Workers:
Fetch page
Parse data
Clean data
Upsert into DB
Log success/failure
Retry failed jobs
🔁 UPDATE STRATEGY
Daily:
Price + ratios
Weekly:
Financials
Monthly:
Full refresh

Use cron or a scheduler.

🛡️ PRODUCTION REQUIREMENTS
Handle failures gracefully
No duplicate inserts (use UPSERT)
Timeout handling
Logging (structured logs)
Config via .env
Modular code (separate services)
📁 PROJECT STRUCTURE
stock_scraper/
│
├── app/
│   ├── scraper/
│   │   ├── screener_scraper.py
│   │   ├── parser.py
│   │   └── cleaner.py
│   │
│   ├── db/
│   │   ├── database.py
│   │   └── models.py
│   │
│   ├── pipeline/
│   │   ├── worker.py
│   │   ├── queue.py
│   │   └── scheduler.py
│   │
│   ├── utils/
│   │   ├── logger.py
│   │   └── retry.py
│
├── scripts/
│   └── run_scraper.py
│
├── .env
├── requirements.txt
└── README.md
🔥 EXTRA (IMPORTANT FOR PRODUCTION)
Use connection pooling
Use bulk inserts where possible
Add indexing on company_id
Use JSONB for flexible schema
Add data versioning (_fetched_at)
🚫 ETHICAL & TECH CONSTRAINTS
Respect Screener rate limits
Add delay between requests
Avoid IP blocking
🧪 OUTPUT
Fully working scraper
Clean structured DB
Logs showing ingestion status
Able to scale to all NSE/BSE companies
🧠 BONUS (IF POSSIBLE)
Add API layer (FastAPI) to query data
Add caching layer (Redis)
Add dashboard later
✅ FINAL INSTRUCTION

Write clean, modular, production-quality code.
Do NOT write a prototype — build it like a real backend system used in a startup.